executor {
    name = 'local'
    exitReadTimeout = '5 min'
    submitRateLimit = '1/1sec'
    queueSize = 100
}

process {
    executor = 'local'

    withLabel: 'interproscan' {
        cpus   = { 16 }
    }
    withLabel: 'gpu' {
        clusterOptions = '--gres=a100:1'
    }
}
