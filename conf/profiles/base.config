executor {
    name = 'local'
    exitReadTimeout = '5 min'
    submitRateLimit = '1/1sec'
    queueSize = 100
}

process {
    executor = 'local'

    withLabel: 'interproscan' {
        cpus   = { 4 }
    }
    withLabel: 'gpu' {
        clusterOptions = '--gres=gpu:a100:1'
    }
}
